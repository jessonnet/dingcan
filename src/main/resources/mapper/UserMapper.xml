<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.canteen.mapper.UserMapper">

    <select id="selectByUsername" parameterType="java.lang.String" resultType="com.canteen.entity.User">
        SELECT id, username, password, name, role_id, department_id, restaurant_id, phone, email, status, created_at, updated_at FROM user WHERE username = #{username}
    </select>

</mapper>
